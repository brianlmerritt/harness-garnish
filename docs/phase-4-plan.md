# Phase 4 plan — budgeted API agents and controlled extensions

## Objective

Phase 4 adds direct OpenAI and Anthropic agent providers without allowing low subscription quota to become implicit paid API spend. API access is disabled by default at both effective-policy and project levels. Enabling a project requires a bounded period, an explicit provider/account and secret reference, at least one hard monetary/token/request ceiling, model/tool/role allowlists, per-request output and retry ceilings, and durable reservation/settlement evidence.

OpenAI targets the Responses API contract. The current official contract returns a response ID, terminal status, model, typed output items, and input/output/total-token usage; streaming uses typed events and may report usage only on the completed response. Harness Garnish therefore reserves the maximum allowed request before network access and settles only from bounded provider evidence. Anthropic follows the equivalent provider-specific adapter contract. Model IDs and prices remain configuration/evidence, not schema constants.

Normal tests use fake transports and fixtures. No test reads a real API key, reaches a provider, or consumes API credit unless it is separately labelled and the user explicitly opts in.

## Implementation stages

1. API budget state: project/provider enablement, bounded periods, secret references, allowed models/tools/roles, output/retry/concurrency ceilings, and append-only configuration evidence.
2. Atomic accounting: reserve worst-case currency/tokens/requests before dispatch; prevent concurrent overcommit; settle actual provider usage once; release failures/cancellation/orphans once.
3. Provider contracts: strict OpenAI Responses and Anthropic response/stream fixture parsers, request IDs, usage, cancellation, rate-limit classification, bounded output, and schema-drift failure.
4. Secret delivery and opt-in smoke tests: protected references only, no secret persistence/logging, explicit disclosed provider/model/budget/network effects, and no automatic CLI-to-API fallback.
5. Routing integration: API candidates pass the same capability, health, policy, schedule, role, and independent-verifier gates plus API-specific budget gates.
6. Controlled extensions: mutually authenticated SSH/Tailscale approval transport, bounded MCP server lifecycle/tool allowlists, reviewed skill attachment, and ACP event mapping without transferring authority to those protocols.
7. Operator interface: read-only API budget/reservation/spend state first; mutations only after CLI parity, CSRF protection, explicit confirmation, and durable action evidence.

## Machine acceptance

| ID | Assertion |
| --- | --- |
| P4-01 | With no enabled project budget, every OpenAI/Anthropic reservation fails before secret access or transport invocation. |
| P4-02 | Enabling requires provider, non-secret secret reference, bounded period, allowed model/role, per-request maximum, reason, and at least one hard currency/token/request ceiling. |
| P4-03 | Subscription quota and API budget remain distinct resources; low subscription quota never creates or selects a paid API route automatically. |
| P4-04 | Monetary values use integer minor/micro units with an explicit currency; no binary floating-point arithmetic decides admission. |
| P4-05 | Concurrent reservations atomically include committed spend plus outstanding reservations and cannot exceed any configured ceiling. |
| P4-06 | A reservation is bound to project/task/provider/model/request digest and is single-settlement; replay or mismatched settlement fails closed. |
| P4-07 | Completion records provider request-ID hash, token categories, actual/estimated cost provenance, and releases unused reservation without storing prompts, output, or secrets. |
| P4-08 | Cancellation, failure, timeout, restart, and orphan recovery release or retain accounting exactly once according to whether provider dispatch was claimed. |
| P4-09 | Model, tool, role, output, retry, concurrency, network, and secret grants are hard filters recorded with stable reason codes. |
| P4-10 | OpenAI Responses fixtures accept documented typed output/usage and completed streaming events, tolerate additive fields, and reject missing, ambiguous, malformed, partial, or drifting required fields. |
| P4-11 | Anthropic fixtures meet the same bounded request-ID, terminal-state, usage, drift, and error-classification contract. |
| P4-12 | Provider rate/usage-limit failures remain distinct from authentication, transient server, policy, and local-budget failures; retries remain bounded and budget-reserved. |
| P4-13 | API keys never enter SQLite, argv, project files, logs, events, patches, fixtures, support bundles, or error messages; canary-secret tests cover every artifact path. |
| P4-14 | Normal macOS/Linux/WSL2 suites use fake transports only and consume no provider subscription quota or paid API credit. |
| P4-15 | Remote approval, MCP, skill, and ACP inputs cannot bypass core policy, action-digest approvals, quota/budget gates, sandbox attestation, or independent verification. |

## Intermediate status

Schema 15 now implements network-free project API budgets and atomic reservations/settlement. Stable CLI JSON plus fake-only default-deny, validation, concurrent overcommit, restart recovery, dispatch, settlement, replay, migration-backup, and request-exhaustion tests pass on macOS. After adding the provider fixture contracts, the 2026-07-20 normal suite passed 115 tests with the two explicitly opt-in real-container tests ignored; it made no provider request and consumed no subscription quota or API credit.

The next network-free slice adds strict OpenAI Responses and Anthropic Messages response/stream fixture parsers. They accept additive non-authoritative fields, require bounded typed output, terminal state, provider request identity, and exact usage, and reject malformed, partial, reordered, ambiguous, or authoritative schema-drift cases. Error fixtures keep authentication, permission, rate limits, paid-usage exhaustion, invalid requests, and transient provider failures distinct. There is still no HTTP transport or secret access, so this slice cannot spend money.

The protected-reference slice centralizes exact `env:NAME`, `file:/absolute/path`, and macOS `keychain:SERVICE/ACCOUNT` locators. Secret values are non-cloneable and non-serializable, have redacted debug output, receive bounded reads, and are cleared on drop. Unix files require the current user, a regular non-symlink file, and no group/other permissions; macOS Keychain lookup uses bounded supervised host execution with locator-only argv. Canary tests cover errors, debug output, SQLite/WAL state, verified backups, diagnostics, and the authenticated UI snapshot. Its macOS suite passed 119 tests with the two real-container tests explicitly ignored. No normal test reads a real credential.

The request-boundary slice constructs only the fixed OpenAI Responses and Anthropic Messages HTTPS endpoints. It repeats active-period, provider, model, tool, and output-ceiling checks before resolving a secret, bounds instructions/input/tools/body, and keeps prompts, outputs, bodies, raw response/request IDs, and authorization material out of `Debug` and serialization. A transport trait receives sensitive parts only through an explicitly named closure; the crate does not yet provide a real HTTP implementation. Fake transport fixtures prove request shapes, header placement, response parsing, and redacted provider failures without network access. Service preparation requires effective-policy permission, a live undispatched reservation, exact provider/model/output/request digest, and the still-latest budget revision. The macOS suite passes 123 tests with the two real-container tests explicitly ignored.

Schema 16 adds append-only provider/account/model/currency price evidence and preserves uncached input, cache-read input, cache-creation input, and output categories. OpenAI's documented `usage.input_tokens_details.cached_tokens` and `cache_write_tokens` meanings are treated as usage evidence, not pricing constants ([official prompt-caching contract](https://developers.openai.com/api/docs/guides/prompt-caching#requirements)). Rates are user-supplied integer currency micros per million tokens; no provider model, price, or cache multiplier is a program constant. Cost is calculated with checked integer arithmetic and a single final upward rounding. Monetary settlement requires the exact price record, verifies its identity and effective interval, recomputes cost, and remains single-use.

The transport-agnostic execution boundary is now complete for fake transports: exact request preparation precedes the dispatch claim; authoritative fixture usage selects the already-verified effective price; the raw provider request ID is hashed; and the reservation settles once. A canary test verifies that secret, prompt, instructions, response text, and raw request ID do not enter SQLite or a verified backup. This is machine evidence for the local accounting lifecycle, not authorization for real network traffic. The crate still has no real HTTP transport.

The 2026-07-20 macOS quota-free suite passed 129 tests with 2 opt-in real-container tests ignored. Strict Clippy with warnings denied also passed. No provider transport, credential, subscription quota, or paid API credit was used.

This is intermediate evidence, not Phase 4 exit. Schema 16 passed the quota-free macOS, native-Linux, and WSL2 suites: 129 tests passed on each platform, with the same 2 explicitly opt-in real-container tests ignored. Exact kernels, toolchains, and safety conditions are recorded in [`phase-4-portability-checkpoint.md`](phase-4-portability-checkpoint.md).

The first routing-integration slice gives the literal `api` adapter its own project-budget capacity gate instead of treating paid capacity as subscription percentage. It requires effective provider policy, the latest enabled and active project budget, implementer-role permission, concurrency and integer currency/token/request headroom, and effective price evidence for at least one allowed model. A mixed subscription/API candidate set marks the API lane `api.explicit_selection_required`; low subscription quota can never select it as fallback. An exact API pin reaches these gates, but automated scheduler claim is intentionally denied as `api.scheduler_execution_unavailable` until the scheduler can atomically bind an exact request digest and API budget reservation. The macOS quota-free suite now passes 132 tests with 2 opt-in real-container tests ignored; strict Clippy also passes.

Schema 17 added that atomic scheduler boundary without enabling network transport. At that checkpoint, exact claims required zero retries and the general daemon denied API candidates until a durable exact-request plan existed. No credential or provider transport was used by that slice.

Schema 18 adds append-only per-task exact request plans and retry-aware reservation accounting. Plans bind the canonical task version, explicit API identity, model, role, per-attempt bounds, retry ceiling, stream setting, deterministic template version, and request digest without storing a duplicate task prompt. Missing, disabled, stale, identity-mismatched, or digest-mismatched plans fail closed. The scheduler can now select an explicitly pinned paid route and atomically claim it while reserving currency, tokens, and request capacity for every permitted attempt. Preparation and settlement retain per-attempt maxima. This implements retry headroom, not an actual retry dispatch loop, and still provides no real HTTP transport or provider credential access.

The 2026-07-20 macOS, native-Linux, and WSL2 quota-free Schema 18 suites each passed 137 tests with the 2 explicitly opt-in real-container tests ignored. Format and strict Clippy passed on all three platforms. Migration-backup, stable CLI JSON, missing/disabled/stale-plan rejection, atomic scheduler binding, and three-attempt worst-case reservation evidence are included. Each suite removed provider credential variables, used fake provider transports, and consumed no subscription/API quota. Exact kernels, toolchains, and the `be6e23b` evidence boundary are recorded in [`phase-4-portability-checkpoint.md`](phase-4-portability-checkpoint.md).

Schema 19 adds a durable dispatch-attempt journal and bounded fake-transport retry execution. Only provider responses classified as rate-limited or transient can advance to another already-reserved attempt. Authentication, permission, usage exhaustion, invalid requests, and other terminal responses stop immediately. Transport errors, malformed successful responses, identity mismatches, and crashes after attempt start remain uncertain and cannot replay automatically. Attempt success and spend settlement commit together, and request-limit accounting retains every started attempt while counting only the unstarted reservation remainder. No response body, raw provider request ID, prompt, or secret is persisted.

The 2026-07-20 macOS, native-Linux, and WSL2 quota-free Schema 19 suites each passed 138 tests with the 2 explicitly opt-in real-container tests ignored; strict Clippy and formatting passed on all three platforms. The fixture matrix proves two retryable failures followed by success, immediate terminal authentication failure, redacted transport uncertainty, restart replay denial, exact request-limit accounting, stable CLI/UI evidence, and verified migration backup. Each suite removed provider credential variables, used fake provider transports, and consumed no subscription/API quota. Exact kernels, toolchains, and the `387d9db` evidence boundary are recorded in [`phase-4-portability-checkpoint.md`](phase-4-portability-checkpoint.md).

The direct transport slice adds a Rustls HTTPS implementation without wiring paid execution into the normal scheduler or CLI. It accepts only the fixed OpenAI Responses and Anthropic Messages endpoints, disables redirects, referrers, proxy inheritance, and Reqwest's implicit protocol retries, and bounds timeouts, response bytes, content types, and provider request IDs. Authorization headers are marked sensitive. The only executable live path is a separately ignored, exactly one-request smoke test requiring a literal paid-request acknowledgement, exact provider/model, and protected credential reference. Normal and portability suites cannot construct that opted-in path and remain quota-free. The 2026-07-20 macOS, native-Linux, and WSL2 quota-free suites each passed 140 tests with 3 explicitly ignored tests: the two real-container checks and the one paid-API smoke test. Strict Clippy and formatting passed on all three platforms. Exact platform evidence and the `5fdbb78` boundary are recorded in [`phase-4-portability-checkpoint.md`](phase-4-portability-checkpoint.md).

The explicitly pinned scheduler-execution slice activates that transport only for a daemon invocation carrying both `--execute-api` and the exact paid-task acknowledgement. Every API candidate now requires an exact task pin, including single-candidate schedulers; the latest plan must use the implementer role and remains bound to the claim's recorded pre-transition task version. This response-only boundary permits only risk-class 0 tasks and records a host-direct-API attestation with no worktree mount rather than claiming fake-container isolation. Claim, run, plan, reservation, provider, account, model, and digest are rechecked before secret resolution. Completed response content is not persisted or applied; the independent verifier evaluates the unchanged isolated worktree. Terminal failures fail the task, while transport or authoritative-response uncertainty retains the dispatch and cannot replay after restart. Fixture tests cover success, settlement, verifier separation, acknowledgement denial, write-risk denial before claim, secret/output/request-ID exclusion, uncertain-dispatch retention, and restart replay denial without provider traffic or paid credit. The 2026-07-21 macOS quota-free suite passed 144 tests with the same 3 explicitly ignored opt-in tests; formatting and strict Clippy passed. ADR 0011 records the activation boundary. Cross-platform evidence for this slice remains required before it can update the Phase 4 portability checkpoint.

The controlled patch-submission slice adds exactly one built-in `submit_patch` tool for explicitly opted-in risk-class 1 tasks. It requires the `agent.patch_submission` capability, an exact task scope, a tool-allowlisted budget, the existing paid-daemon acknowledgement, and a second session acknowledgement. The provider receives no execution or repository access. Garnish bounds and validates the UTF-8 git diff, applies it only to a clean isolated task worktree, then rejects any changed path outside exact scope before handing the patch to a separate detached verifier. Binary data, links, submodules, renames, copies, malformed or multiple tool results, risk classes 2/3, and disabled branch policy fail closed. Fake OpenAI and Anthropic end-to-end fixtures exercise the accepted path; deterministic rejection fixtures cover acknowledgement, patch shape, clean-worktree, type, and scope gates. A separately ignored one-request real-provider patch smoke uses only temporary state and requires `I_ACCEPT_ONE_PAID_API_PATCH_REQUEST`. ADR 0012 records the narrow class-1 policy exception. The 2026-07-21 final-tree macOS quota-free suite passed 150 tests with 4 explicitly ignored opt-in tests; provider credentials and both paid-smoke acknowledgements were removed, and formatting, script syntax, and strict Clippy passed. No provider request or paid credit was consumed. Cross-platform evidence and an explicitly chosen paid smoke run remain separate checkpoints.

The operator subsequently reported that both the response-only and isolated-patch smoke scripts passed once for OpenAI and once for Anthropic: four bounded live requests with zero application retries. Because the original scripts intentionally discarded their temporary state and emitted no durable receipt, [`phase-4-live-api-checkpoint.md`](phase-4-live-api-checkpoint.md) labels this as operator-reported evidence rather than Phase 4 exit evidence. Future successful smoke runs now emit a redacted private receipt only after the exact ignored test passes.

The post-smoke evidence hardening is quota-free: a validated helper writes a private, no-clobber JSON receipt containing only non-secret test identity and fixed limits. Unsafe provider/model values fail before the receipt write, and fixture coverage parses both response and patch receipts while checking permissions and sensitive-field absence. The updated macOS suite passes 151 tests with the same 4 explicitly ignored external tests; formatting, shell syntax, and strict Clippy pass with provider credentials and acknowledgements removed.

Schema 20 begins P4-15 with a deliberately non-executing MCP registration boundary. Project-scoped append-only revisions pin the local executable path and SHA-256, argv data, exact tool names, exact host and protected-secret-reference grants, bounded startup/request timeouts, bounded context/output sizes, source, reason, and default-disabled state. Stable CLI JSON can configure and inspect this state, but there is no server launch, protocol exchange, secret resolution, tool authorization, discovery, or installation path. ADR 0013 records why administrative enablement is necessary but not sufficient. The 2026-07-21 macOS quota-free suite passed 154 tests with the same 4 explicitly ignored external tests; formatting and strict Clippy passed with provider credentials and paid acknowledgements removed. This is initial controlled-extension evidence, not completion of P4-15.

The CLI MVP slice adds a separate one-task Codex subscription patch mode while retaining the existing explicitly budgeted OpenAI/Anthropic API modes. An exact Codex task pin, fresh supported probe, subscription quota headroom, risk class 1, exact scope, one route candidate, one active claim, and the literal `I_ACCEPT_ONE_CODEX_SUBSCRIPTION_TASK` are required. Codex runs ephemerally with JSONL and read-only command permissions; user configuration, exec-policy rules, MCP, apps, hooks, plugins, multi-agent operation, web search, and inherited environment are disabled. Repository content and project instructions remain untrusted model input and cannot expand process or patch authority. Garnish accepts one final patch, applies it only to the isolated worktree, and uses the existing detached verifier and new stable `task review` projection. The fake CLI fixture proves exact-scope output, checkpoint renewal, one-task daemon stop, and absence of reasoning/stderr canaries from durable artifacts. A separately ignored `scripts/test-real-codex-subscription-smoke` path creates only temporary state and requires an immediately observed quota percentage plus the literal acknowledgement; a passing run writes a private redacted receipt. The 2026-07-21 macOS quota-free suite passed 159 tests with 5 explicitly ignored external tests; formatting and strict Clippy passed with provider credentials and paid acknowledgements removed. No subscription task or API request was made. ADR 0014 records the execution boundary.

The first opted-in live attempt used Codex CLI 0.144.6 and a valid immediately observed quota value. It submitted exactly one acknowledged temporary task and ended cleanly after 12.53 seconds, but Garnish rejected the result before patch application because the CLI emitted more than one completed `agent_message`. No receipt was emitted, the registered source checkout remained outside the write path, and this is failed compatibility evidence rather than live conformance. The parser regression permits bounded non-patch progress messages while requiring the last agent message to be the sole patch-bearing message.

The subsequent explicitly acknowledged Codex 0.144.6 smoke passed in 12.67 seconds. It exercised one temporary subscription task with zero retries, exact `result.txt` scope, read-only Codex commands, isolated patch application, and separate detached-worktree verification. The registered source checkout remained unchanged, raw model output was not persisted, and a private mode-0600 redacted receipt was emitted only after the exact ignored test passed. [`phase-4-live-codex-checkpoint.md`](phase-4-live-codex-checkpoint.md) records the evidence without storing the operator's quota value. This establishes live macOS conformance for the narrow Codex subscription patch boundary. Cross-platform evidence remains outstanding, so this is not Phase 4 exit evidence.

The CLI workflow hardening makes placeholder substitution explicit rather than relying on uppercase tokens or surrounding prose. Every copyable console block declares either no placeholders or the exact values to replace and their source; a quota-free script enforces this convention in CI and portability runs. The portability scrub now also removes Codex acknowledgement, quota, and receipt variables plus every API and real-container selector. The final macOS quota-free suite remains at 159 passed with 5 external tests ignored, with formatting, strict Clippy, shell syntax, and placeholder checks clean. The native-Linux and WSL2 results in the existing portability checkpoint predate this slice and remain pending revalidation.

## Explicit non-goals

Phase 4 does not enable API access merely because an environment variable exists, infer consent from an old prepaid balance, hard-code a current model alias or price, send a real request during normal tests, auto-install skills/MCP servers, broaden the local UI bind address, or permit remote approval to become an unbounded reusable authority. Packaging, signed self-update activation, Apple Container general support, encrypted portable export, and automatic Git integration remain later work.
